{
  "name": "washwise-carwash-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@washwise/web",
    "dev:api": "npm run dev --workspace=@washwise/api",
    "build": "npm run build --workspace=@washwise/api && npm run build --workspace=@washwise/web",
    "lint": "npm run lint --workspace=@washwise/api && npm run lint --workspace=@washwise/web",
    "test": "npm run test --workspace=@washwise/api"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
